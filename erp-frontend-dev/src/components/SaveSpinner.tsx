import { FaSpinner } from 'react-icons/fa';

const SaveSpinner = () => {
    return (
        <FaSpinner
            className='spin'
            style={{
                marginRight: '0.5rem'
            }}
        />
    )
};

export default SaveSpinner;
